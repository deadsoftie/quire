{
  "name": "quire",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently -k -s first \"pnpm --filter @quire/ui dev\" \"wait-on http://localhost:5173 && pnpm --filter @quire/desktop dev\"",
    "package": "bash scripts/package.sh"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "wait-on": "^8.0.0"
  }
}
