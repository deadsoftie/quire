[package]
name = "quire-sidecar"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
quire-core = { path = "../quire-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
