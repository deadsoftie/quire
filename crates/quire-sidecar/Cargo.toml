[package]
name = "quire-sidecar"
version.workspace = true
edition = "2021"
license = "MIT"

[dependencies]
quire-core = { path = "../quire-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
