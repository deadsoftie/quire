\documentclass{article}
\begin{document}
A bare article with no font/package choices at all -- this is what Tectonic's own default
LaTeX format falls back to (native Unicode Latin Modern, no \texttt{fontenc}/\texttt{lmodern}
override), and it's the most common state an in-progress document is actually in.

Some \textbf{bold}, \textit{italic}, and \emph{emphasized} text.

An undefined cross-reference: Figure~\ref{fig:does-not-exist}.

An undefined citation: \cite{does-not-exist-2024}.
\end{document}
