\section{Chapter 31}
\label{sec:ch31}

This is chapter 31 of the thesis. It defines $\vect{x}_{31}$ and $\norm{\vect{x}_{31}}$, and cites~\cite{ref1}.

This paragraph exists to give the chapter realistic bulk, the same way an actual thesis chapter would carry several paragraphs of exposition between its headings rather than a single terse sentence. Incremental reindexing should stay fast whether a file is short or long, so padding this out is deliberate, not filler for its own sake.

\subsection{Background}
\label{sec:ch31:background}

This paragraph exists to give the chapter realistic bulk, the same way an actual thesis chapter would carry several paragraphs of exposition between its headings rather than a single terse sentence. Incremental reindexing should stay fast whether a file is short or long, so padding this out is deliberate, not filler for its own sake.

See Section~\ref{sec:ch30} for the previous chapter.

\subsection{Analysis}
\label{sec:ch31:analysis}

This paragraph exists to give the chapter realistic bulk, the same way an actual thesis chapter would carry several paragraphs of exposition between its headings rather than a single terse sentence. Incremental reindexing should stay fast whether a file is short or long, so padding this out is deliberate, not filler for its own sake.

This paragraph exists to give the chapter realistic bulk, the same way an actual thesis chapter would carry several paragraphs of exposition between its headings rather than a single terse sentence. Incremental reindexing should stay fast whether a file is short or long, so padding this out is deliberate, not filler for its own sake.
