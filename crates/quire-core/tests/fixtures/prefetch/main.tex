\documentclass{article}
\usepackage{media9}
\begin{document}
Prefetch test fixture -- media9 is deliberately not in the core bundle, so this exercises
a real network fetch. See tests/rpc_handlers.rs.
\end{document}
