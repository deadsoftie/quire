\documentclass{article}
\usepackage{this-package-definitely-does-not-exist-anywhere}
\begin{document}
Missing-package test fixture -- the package name above is deliberately nonexistent, so this
should fail identically online or offline. See tests/rpc_handlers.rs.
\end{document}
